# Project-specific Codex configuration belongs here. CLI profiles are user-scoped
# in current Codex: ~/.codex/build.config.toml and
# ~/.codex/verifier.config.toml. Keeping profiles out of this project file prevents
# a false impression that `codex --profile ...` reads legacy [profiles.*] tables.
